{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window",
  "windows": ["main", "quick-menu"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title"
  ]
}
